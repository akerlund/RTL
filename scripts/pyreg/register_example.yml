---
exa:
  base_addr: "'h00000000"
  bus_width: 32
  acronym: "EXA"

  registers:

    # Comment
    - name:   cr_configuration
      desc:   DESCRIPTION
      access: RW
      bit_fields:
        - field: {
          name:        cr_configuration,
          description: DESCRIPTION,
          size:        CR_SIZE_C,
          lsb_pos:     0,
          reset_value: 0x0
        }

    #
    - name:   sr_status
      desc:   DESCRIPTION
      access: RO
      bit_fields:
        - field: {
          name:        sr_status,
          description: DESCRIPTION,
          size:        SR_SIZE_C,
          lsb_pos:     0
        }

    #
    - name:   cmd_command
      desc:   DESCRIPTION
      access: WO
      bit_fields:
        - field: {
          name:        cmd_command,
          description: DESCRIPTION,
          size:        1,
          lsb_pos:     0,
          reset_value: 0x0
        }